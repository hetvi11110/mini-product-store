import { ExactWordLimitPipe } from './exact-word-limit.pipe';

describe('ExactWordLimitPipe', () => {
  it('create an instance', () => {
    const pipe = new ExactWordLimitPipe();
    expect(pipe).toBeTruthy();
  });
});
